# UTF-8
#
# Windows version resource template for Tangerine.exe.
#
# This file is NOT used directly: packaging\tangerine.spec reads it at build
# time, replaces the @@VERSION_TUPLE@@ / @@VERSION@@ placeholders with the
# current APP_VERSION from tangerine\paths.py, and writes the result to
# packaging\_version_info_generated.txt (which is what PyInstaller consumes).
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=@@VERSION_TUPLE@@,
    prodvers=@@VERSION_TUPLE@@,
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo([
      StringTable(
        '040904B0',
        [StringStruct('CompanyName', 'Tangerine for Windows contributors'),
         StringStruct('FileDescription', 'Tangerine for Windows'),
         StringStruct('FileVersion', '@@VERSION@@'),
         StringStruct('InternalName', 'Tangerine'),
         StringStruct('OriginalFilename', 'Tangerine.exe'),
         StringStruct('ProductName', 'Tangerine for Windows'),
         StringStruct('ProductVersion', '@@VERSION@@')])
    ]),
    VarFileInfo([VarStruct('Translation', [1033, 1200])])
  ]
)
